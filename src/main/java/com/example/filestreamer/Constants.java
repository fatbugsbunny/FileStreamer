package com.example.filestreamer;

public enum Constants {
    ;

    public enum ServerActions {
        REQUEST_FILE_LIST,
        ADD_CLIENT,
        GET_KNOWN_CLIENTS,
        UPLOAD, GET_MESSAGE, INITIATE_CHAT, REQUEST_NAME, GET_CHAT_CONNECTIONS, GET_CLIENTS_CLIENTS, CLOSE, DOWNLOAD
    }
}
